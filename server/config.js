const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "haha",
      password: "aknakn0091",
      database: "aungkyawnyunt.com",
    },
    listPerPage: 10,
  };
  module.exports = config;